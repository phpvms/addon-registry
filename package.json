{
	"name": "@phpvms/addon-registry",
	"version": "0.0.0",
	"private": true,
	"description": "phpVMS addon registry — curated catalogue validated against a JSON schema",
	"type": "module",
	"engines": {
		"bun": ">=1.1"
	},
	"scripts": {
		"validate": "registry-validate",
		"publish": "registry-publish",
		"typecheck": "tsc --noEmit",
		"test": "bun test",
		"format": "prettier --write ."
	},
	"dependencies": {
		"@phpvms/registry-client": "^0.1.0"
	},
	"devDependencies": {
		"@types/bun": "^1.1.10",
		"@types/node": "^22.7.5",
		"prettier": "^3.3.3",
		"typescript": "^5.6.2"
	}
}
